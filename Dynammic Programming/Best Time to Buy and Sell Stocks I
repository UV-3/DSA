int Solution::maxProfit(const vector<int> &A) {
    int ans=0;
    if(A.size()==0 || A.size()==1){
        return ans;
    }
    int mn=A[0];
    for(int i=1;i<A.size();i++){
        ans= max(ans,A[i]-mn);
        mn=min(mn,A[i]);
    }
    return ans;
}
